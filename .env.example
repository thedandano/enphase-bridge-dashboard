# Required: URL of the enphase-bridge API server
# On Linux/macOS with Docker, use host.docker.internal for the host machine
BRIDGE_API_URL=http://host.docker.internal:8080

# Optional: API key for enphase-bridge if api_key is set in bridge config.toml
# Leave empty if the bridge does not require authentication
BRIDGE_API_KEY=
